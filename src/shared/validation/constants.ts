const ERROR_MESSAGES = {
  required: "required",
  notEmptyString: "must be not empty string",
  base64ImageDataUrl: "must be base64 image in data URL format with mediatype",
} as const;

export { ERROR_MESSAGES };
